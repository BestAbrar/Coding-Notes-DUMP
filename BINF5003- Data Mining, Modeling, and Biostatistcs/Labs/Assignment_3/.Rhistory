#'deviation between the two variables
library(ggplot2) # plotting
library(dplyr) # manipulation
ggplot(bCancer, aes(x=diagnosis,y=radius_mean))+
geom_violin(aes(fill=diagnosis))+
geom_boxplot(width=0.1)+
labs(title="Average Mean Radius by Diagnosis",
x="Diagnosis", y="Average radius (mm)")
#'b)I would use a scater plot to compare 2 continuous variables, I can then use
#'smooth to determine if there is a correlation between the two variables
ggplot(bCancer,aes(x=smoothness_mean,y=compactness_mean))+
geom_point(aes(color=diagnosis))+
geom_smooth(method="lm", se =FALSE)+
labs(title="Mean Smoothness vs Compatness",
x="Mean Smoothness",
y="Mean Compactness",)
model <- lm(smoothness_mean ~ compactness_mean, data = bCancer)
intercept <- coef(model)[1]
slope <- coef(model)[2]
r2 <- summary(model)$r.squared
pvalue <- summary(model)$coefficients[2,4]
#based on the data, the correlation is significant (non-random), weak and positive between the Mean smoothness and the Mean Compactness
print(intercept)
print(slope)
print(r2)
print(pvalue)
#'C) The form of data needed for this plot is a dataframe, with variables
#''diagnosis, varibles(consisting of 'radius_mean' and 'texture_mean') and their
#'corresponding values
bCancerLong<- bCancer%>%
select(diagnosis,radius_mean,texture_mean) %>%
pivot_longer(cols = "radius_mean":"texture_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLong, aes(x=diagnosis,y=values))+
geom_violin(aes(fill=diagnosis))+
facet_wrap(~variables)
#'d)
bCancerLongest<-bCancer %>%
select(diagnosis:symmetry_mean) %>%
pivot_longer(cols = "radius_mean":"symmetry_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLong, aes(x=diagnosis,y=values))+
geom_histogram(aes(fill=diagnosis))+
facet_wrap(~variables)
ggplot(bCancerLongest, aes(x=diagnosis,y=values))+
geom_histogram(aes(fill=diagnosis))+
facet_wrap(~variables)
ggplot(bCancerLongest, aes(x=diagnosis,y=..density..))+
geom_histogram(aes(fill=diagnosis))+
facet_wrap(~variables)
ggplot(bCancerLongest, aes(x=variables,y=..density..))+
geom_histogram(aes(fill=diagnosis))+
facet_wrap(~variables)
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis))+
facet_wrap(~variables)
#'a) I would use a box plot (with optional violin plot to show density
#'distribution, they both compliment the shortcomings of each other well), the data
#'is comparing the mean radius BETWEEN two categorical
#'variables:'M' and 'B' while also showing the distribution and the standard
#'deviation between the two variables
library(ggplot2) # plotting
library(dplyr) # manipulation
ggplot(bCancer, aes(x=diagnosis,y=radius_mean))+
geom_violin(aes(fill=diagnosis))+
geom_boxplot(width=0.1)+
labs(title="Average Mean Radius by Diagnosis",
x="Diagnosis", y="Average radius (mm)")
#'b)I would use a scater plot to compare 2 continuous variables, I can then use
#'smooth to determine if there is a correlation between the two variables
ggplot(bCancer,aes(x=smoothness_mean,y=compactness_mean))+
geom_point(aes(color=diagnosis))+
geom_smooth(method="lm", se =FALSE)+
labs(title="Mean Smoothness vs Compatness",
x="Mean Smoothness",
y="Mean Compactness",)
model <- lm(smoothness_mean ~ compactness_mean, data = bCancer)
intercept <- coef(model)[1]
slope <- coef(model)[2]
r2 <- summary(model)$r.squared
pvalue <- summary(model)$coefficients[2,4]
#based on the data, the correlation is significant (non-random), weak and positive between the Mean smoothness and the Mean Compactness
print(intercept)
print(slope)
print(r2)
print(pvalue)
#'C) The form of data needed for this plot is a dataframe, with variables
#''diagnosis, varibles(consisting of 'radius_mean' and 'texture_mean') and their
#'corresponding values
bCancerLong<- bCancer%>%
select(diagnosis,radius_mean,texture_mean) %>%
pivot_longer(cols = "radius_mean":"texture_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLong, aes(x=diagnosis,y=values))+
geom_violin(aes(fill=diagnosis))+
facet_wrap(~variables)
#'d)
bCancerLongest<-bCancer %>%
select(diagnosis:symmetry_mean) %>%
pivot_longer(cols = "radius_mean":"symmetry_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis),scales='free_x')+
facet_wrap(~variables)
#'a) I would use a box plot (with optional violin plot to show density
#'distribution, they both compliment the shortcomings of each other well), the data
#'is comparing the mean radius BETWEEN two categorical
#'variables:'M' and 'B' while also showing the distribution and the standard
#'deviation between the two variables
library(ggplot2) # plotting
library(dplyr) # manipulation
ggplot(bCancer, aes(x=diagnosis,y=radius_mean))+
geom_violin(aes(fill=diagnosis))+
geom_boxplot(width=0.1)+
labs(title="Average Mean Radius by Diagnosis",
x="Diagnosis", y="Average radius (mm)")
#'b)I would use a scater plot to compare 2 continuous variables, I can then use
#'smooth to determine if there is a correlation between the two variables
ggplot(bCancer,aes(x=smoothness_mean,y=compactness_mean))+
geom_point(aes(color=diagnosis))+
geom_smooth(method="lm", se =FALSE)+
labs(title="Mean Smoothness vs Compatness",
x="Mean Smoothness",
y="Mean Compactness",)
model <- lm(smoothness_mean ~ compactness_mean, data = bCancer)
intercept <- coef(model)[1]
slope <- coef(model)[2]
r2 <- summary(model)$r.squared
pvalue <- summary(model)$coefficients[2,4]
#based on the data, the correlation is significant (non-random), weak and positive between the Mean smoothness and the Mean Compactness
print(intercept)
print(slope)
print(r2)
print(pvalue)
#'C) The form of data needed for this plot is a dataframe, with variables
#''diagnosis, varibles(consisting of 'radius_mean' and 'texture_mean') and their
#'corresponding values
bCancerLong<- bCancer%>%
select(diagnosis,radius_mean,texture_mean) %>%
pivot_longer(cols = "radius_mean":"texture_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLong, aes(x=diagnosis,y=values))+
geom_violin(aes(fill=diagnosis))+
facet_wrap(~variables)
#'d)
bCancerLongest<-bCancer %>%
select(diagnosis:symmetry_mean) %>%
pivot_longer(cols = "radius_mean":"symmetry_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis))+
facet_wrap(~variables,scales='free_x')
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis))+
facet_wrap(~variables,scales='free_x',scales='free_y')
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis))+
facet_wrap(~variables,scales='free')
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis),alpha=0.6, position = 'identity')+
facet_wrap(~variables,scales='free')
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis),alpha=0.6)+
facet_wrap(~variables,scales='free')
#'a) I would use a box plot (with optional violin plot to show density
#'distribution, they both compliment the shortcomings of each other well), the data
#'is comparing the mean radius BETWEEN two categorical
#'variables:'M' and 'B' while also showing the distribution and the standard
#'deviation between the two variables
library(ggplot2) # plotting
library(dplyr) # manipulation
ggplot(bCancer, aes(x=diagnosis,y=radius_mean))+
geom_violin(aes(fill=diagnosis))+
geom_boxplot(width=0.1)+
labs(title="Average Mean Radius by Diagnosis",
x="Diagnosis", y="Average radius (mm)")
#'b)I would use a scater plot to compare 2 continuous variables, I can then use
#'smooth to determine if there is a correlation between the two variables
ggplot(bCancer,aes(x=smoothness_mean,y=compactness_mean))+
geom_point(aes(color=diagnosis))+
geom_smooth(method="lm", se =FALSE)+
labs(title="Mean Smoothness vs Compatness",
x="Mean Smoothness",
y="Mean Compactness",)
model <- lm(smoothness_mean ~ compactness_mean, data = bCancer)
intercept <- coef(model)[1]
slope <- coef(model)[2]
r2 <- summary(model)$r.squared
pvalue <- summary(model)$coefficients[2,4]
#based on the data, the correlation is significant (non-random), weak and positive between the Mean smoothness and the Mean Compactness
print(intercept)
print(slope)
print(r2)
print(pvalue)
#'C) The form of data needed for this plot is a dataframe, with variables
#''diagnosis, varibles(consisting of 'radius_mean' and 'texture_mean') and their
#'corresponding values
bCancerLong<- bCancer%>%
select(diagnosis,radius_mean,texture_mean) %>%
pivot_longer(cols = "radius_mean":"texture_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLong, aes(x=diagnosis,y=values))+
geom_violin(aes(fill=diagnosis))+
facet_wrap(~variables)
#'d)
bCancerLongest<-bCancer %>%
select(diagnosis:symmetry_mean) %>%
pivot_longer(cols = "radius_mean":"symmetry_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis),alpha=0.6, position = 'identity')+
facet_wrap(~variables,scales='free')
View(model)
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis),alpha=0.6, position = 'identity')+
facet_wrap(~variables,scales='free')
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis),alpha=0.6, position = 'identity')+
facet_wrap(~variables,scales='free')
#'a) I would use a box plot (with optional violin plot to show density
#'distribution, they both compliment the shortcomings of each other well), the data
#'is comparing the mean radius BETWEEN two categorical
#'variables:'M' and 'B' while also showing the distribution and the standard
#'deviation between the two variables
library(ggplot2) # plotting
library(dplyr) # manipulation
ggplot(bCancer, aes(x=diagnosis,y=radius_mean))+
geom_violin(aes(fill=diagnosis))+
geom_boxplot(width=0.1)+
labs(title="Average Mean Radius by Diagnosis",
x="Diagnosis", y="Average radius (mm)")
#'b)I would use a scater plot to compare 2 continuous variables, I can then use
#'smooth to determine if there is a correlation between the two variables
ggplot(bCancer,aes(x=smoothness_mean,y=compactness_mean))+
geom_point(aes(color=diagnosis))+
geom_smooth(method="lm", se =FALSE)+
labs(title="Mean Smoothness vs Compatness",
x="Mean Smoothness",
y="Mean Compactness",)
model <- lm(smoothness_mean ~ compactness_mean, data = bCancer)
intercept <- coef(model)[1]
slope <- coef(model)[2]
r2 <- summary(model)$r.squared
pvalue <- summary(model)$coefficients[2,4]
#based on the data, the correlation is significant (non-random), weak and positive between the Mean smoothness and the Mean Compactness
print(intercept)
print(slope)
print(r2)
print(pvalue)
#'C) The form of data needed for this plot is a dataframe, with variables
#''diagnosis, varibles(consisting of 'radius_mean' and 'texture_mean') and their
#'corresponding values
bCancerLong<- bCancer%>%
select(diagnosis,radius_mean,texture_mean) %>%
pivot_longer(cols = "radius_mean":"texture_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLong, aes(x=diagnosis,y=values))+
geom_violin(aes(fill=diagnosis))+
facet_wrap(~variables)
#'d)
bCancerLongest<-bCancer %>%
select(diagnosis:symmetry_mean) %>%
pivot_longer(cols = "radius_mean":"symmetry_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis,'M+B'= "#62b6b6"),alpha=0.6, position = 'identity')+
facet_wrap(~variables,scales='free')
labs(aes(legend='M','B','M+B'))
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis,'M+B'= "#62b6b6"),alpha=0.6, position = 'identity')+
facet_wrap(~variables,scales='free')
#'a) I would use a box plot (with optional violin plot to show density
#'distribution, they both compliment the shortcomings of each other well), the data
#'is comparing the mean radius BETWEEN two categorical
#'variables:'M' and 'B' while also showing the distribution and the standard
#'deviation between the two variables
library(ggplot2) # plotting
library(dplyr) # manipulation
ggplot(bCancer, aes(x=diagnosis,y=radius_mean))+
geom_violin(aes(fill=diagnosis))+
geom_boxplot(width=0.1)+
labs(title="Average Mean Radius by Diagnosis",
x="Diagnosis", y="Average radius (mm)")
#'b)I would use a scater plot to compare 2 continuous variables, I can then use
#'smooth to determine if there is a correlation between the two variables
ggplot(bCancer,aes(x=smoothness_mean,y=compactness_mean))+
geom_point(aes(color=diagnosis))+
geom_smooth(method="lm", se =FALSE)+
labs(title="Mean Smoothness vs Compatness",
x="Mean Smoothness",
y="Mean Compactness",)
model <- lm(smoothness_mean ~ compactness_mean, data = bCancer)
intercept <- coef(model)[1]
slope <- coef(model)[2]
r2 <- summary(model)$r.squared
pvalue <- summary(model)$coefficients[2,4]
#based on the data, the correlation is significant (non-random), weak and positive between the Mean smoothness and the Mean Compactness
print(intercept)
print(slope)
print(r2)
print(pvalue)
#'C) The form of data needed for this plot is a dataframe, with variables
#''diagnosis, varibles(consisting of 'radius_mean' and 'texture_mean') and their
#'corresponding values
bCancerLong<- bCancer%>%
select(diagnosis,radius_mean,texture_mean) %>%
pivot_longer(cols = "radius_mean":"texture_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLong, aes(x=diagnosis,y=values))+
geom_violin(aes(fill=diagnosis))+
facet_wrap(~variables)
#'d)
bCancerLongest<-bCancer %>%
select(diagnosis:symmetry_mean) %>%
pivot_longer(cols = "radius_mean":"symmetry_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis,'M+B'= "#62b6b6"),alpha=0.6, position = 'identity')+
facet_wrap(~variables,scales='free')
#'a) I would use a box plot (with optional violin plot to show density
#'distribution, they both compliment the shortcomings of each other well), the data
#'is comparing the mean radius BETWEEN two categorical
#'variables:'M' and 'B' while also showing the distribution and the standard
#'deviation between the two variables
library(ggplot2) # plotting
library(dplyr) # manipulation
ggplot(bCancer, aes(x=diagnosis,y=radius_mean))+
geom_violin(aes(fill=diagnosis))+
geom_boxplot(width=0.1)+
labs(title="Average Mean Radius by Diagnosis",
x="Diagnosis", y="Average radius (mm)")
#'b)I would use a scater plot to compare 2 continuous variables, I can then use
#'smooth to determine if there is a correlation between the two variables
ggplot(bCancer,aes(x=smoothness_mean,y=compactness_mean))+
geom_point(aes(color=diagnosis))+
geom_smooth(method="lm", se =FALSE)+
labs(title="Mean Smoothness vs Compatness",
x="Mean Smoothness",
y="Mean Compactness",)
model <- lm(smoothness_mean ~ compactness_mean, data = bCancer)
intercept <- coef(model)[1]
slope <- coef(model)[2]
r2 <- summary(model)$r.squared
pvalue <- summary(model)$coefficients[2,4]
#based on the data, the correlation is significant (non-random), weak and positive between the Mean smoothness and the Mean Compactness
print(intercept)
print(slope)
print(r2)
print(pvalue)
#'C) The form of data needed for this plot is a dataframe, with variables
#''diagnosis, varibles(consisting of 'radius_mean' and 'texture_mean') and their
#'corresponding values
bCancerLong<- bCancer%>%
select(diagnosis,radius_mean,texture_mean) %>%
pivot_longer(cols = "radius_mean":"texture_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLong, aes(x=diagnosis,y=values))+
geom_violin(aes(fill=diagnosis))+
facet_wrap(~variables)
#'d)
bCancerLongest<-bCancer %>%
select(diagnosis:symmetry_mean) %>%
pivot_longer(cols = "radius_mean":"symmetry_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(mapping = aes(fill=diagnosis,'M+B'= "#62b6b6"),alpha=0.6, position = 'identity')+
facet_wrap(~variables,scales='free')
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis,'M+B'= "#62b6b6"),alpha=0.6, position = 'identity')+
facet_wrap(~variables,scales='free')
#'a) I would use a box plot (with optional violin plot to show density
#'distribution, they both compliment the shortcomings of each other well), the data
#'is comparing the mean radius BETWEEN two categorical
#'variables:'M' and 'B' while also showing the distribution and the standard
#'deviation between the two variables
library(ggplot2) # plotting
library(dplyr) # manipulation
ggplot(bCancer, aes(x=diagnosis,y=radius_mean))+
geom_violin(aes(fill=diagnosis))+
geom_boxplot(width=0.1)+
labs(title="Average Mean Radius by Diagnosis",
x="Diagnosis", y="Average radius (mm)")
#'b)I would use a scater plot to compare 2 continuous variables, I can then use
#'smooth to determine if there is a correlation between the two variables
ggplot(bCancer,aes(x=smoothness_mean,y=compactness_mean))+
geom_point(aes(color=diagnosis))+
geom_smooth(method="lm", se =FALSE)+
labs(title="Mean Smoothness vs Compatness",
x="Mean Smoothness",
y="Mean Compactness",)
model <- lm(smoothness_mean ~ compactness_mean, data = bCancer)
intercept <- coef(model)[1]
slope <- coef(model)[2]
r2 <- summary(model)$r.squared
pvalue <- summary(model)$coefficients[2,4]
#based on the data, the correlation is significant (non-random), weak and positive between the Mean smoothness and the Mean Compactness
print(intercept)
print(slope)
print(r2)
print(pvalue)
#'C) The form of data needed for this plot is a dataframe, with variables
#''diagnosis, varibles(consisting of 'radius_mean' and 'texture_mean') and their
#'corresponding values
bCancerLong<- bCancer%>%
select(diagnosis,radius_mean,texture_mean) %>%
pivot_longer(cols = "radius_mean":"texture_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLong, aes(x=diagnosis,y=values))+
geom_violin(aes(fill=diagnosis))+
facet_wrap(~variables)
#'d)
bCancerLongest<-bCancer %>%
select(diagnosis:symmetry_mean) %>%
pivot_longer(cols = "radius_mean":"symmetry_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis),alpha=0.6, position = 'identity')+
labs(legend=aes(fill=diagnosis,'M+B'= "#62b6b6"))
facet_wrap(~variables,scales='free')
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis),alpha=0.6, position = 'identity')+
labs(legend=aes(fill=diagnosis,'M+B'= "#62b6b6"))+
facet_wrap(~variables,scales='free')
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis),alpha=0.6, position = 'identity')+
scale_color_manual(color=diagnosis,'M+B'= "#62b6b6")
#'d)
bCancerLongest<-bCancer %>%
select(diagnosis:symmetry_mean) %>%
pivot_longer(cols = "radius_mean":"symmetry_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis),alpha=0.6, position = 'identity')+
scale_color_manual(color=diagnosis,'M+B'= "#62b6b6")
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis),alpha=0.6, position = 'identity')+
#scale_color_manual(color=diagnosis,'M+B'= "#62b6b6")
facet_wrap(~variables,scales='free')
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis),alpha=0.6, position = 'identity')+
scale_color_manual(aes(color=diagnosis),'M+B'= "#62b6b6")
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis),alpha=0.6, position = 'identity')+
facet_wrap(~variables,scales='free')
#'a) I would use a box plot (with optional violin plot to show density
#'distribution, they both compliment the shortcomings of each other well), the data
#'is comparing the mean radius BETWEEN two categorical
#'variables:'M' and 'B' while also showing the distribution and the standard
#'deviation between the two variables
library(ggplot2) # plotting
library(dplyr) # manipulation
ggplot(bCancer, aes(x=diagnosis,y=radius_mean))+
geom_violin(aes(fill=diagnosis))+
geom_boxplot(width=0.1)+
labs(title="Average Mean Radius by Diagnosis",
x="Diagnosis", y="Average radius (mm)")
#'b)I would use a scater plot to compare 2 continuous variables, I can then use
#'smooth to determine if there is a correlation between the two variables
ggplot(bCancer,aes(x=smoothness_mean,y=compactness_mean))+
geom_point(aes(color=diagnosis))+
geom_smooth(method="lm", se =FALSE)+
labs(title="Mean Smoothness vs Compatness",
x="Mean Smoothness",
y="Mean Compactness",)
model <- lm(smoothness_mean ~ compactness_mean, data = bCancer)
intercept <- coef(model)[1]
slope <- coef(model)[2]
r2 <- summary(model)$r.squared
pvalue <- summary(model)$coefficients[2,4]
#based on the data, the correlation is significant (non-random), weak and positive between the Mean smoothness and the Mean Compactness
print(intercept)
print(slope)
print(r2)
print(pvalue)
#'C) The form of data needed for this plot is a dataframe, with variables
#''diagnosis, varibles(consisting of 'radius_mean' and 'texture_mean') and their
#'corresponding values
bCancerLong<- bCancer%>%
select(diagnosis,radius_mean,texture_mean) %>%
pivot_longer(cols = "radius_mean":"texture_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLong, aes(x=diagnosis,y=values))+
geom_violin(aes(fill=diagnosis))+
facet_wrap(~variables)
#'d)
bCancerLongest<-bCancer %>%
select(diagnosis:symmetry_mean) %>%
pivot_longer(cols = "radius_mean":"symmetry_mean",
names_to = "variables",
values_to = "values")
ggplot(bCancerLongest, aes(x=values,y=..density..))+
geom_histogram(aes(fill=diagnosis),alpha=0.6, position = 'identity')+
labs(title='bCancer mean values distribution summary')+
facet_wrap(~variables,scales='free')
